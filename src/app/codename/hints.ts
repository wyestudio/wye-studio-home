import "server-only";

/**
 * /codename 힌트 원문 — **서버에만 둔다.**
 *
 * ⚠️ content.ts 로 옮기지 않는다. content.ts 는 클라이언트 컴포넌트가 import 하므로
 *    브라우저 번들에 통째로 실린다. 그러면 "순서대로만 열린다" 는 잠금이 화면에서만
 *    걸린 셈이 되어, 개발자도구로 잠긴 힌트까지 그대로 읽힌다(2026-09-19 확인).
 *    열람 버튼을 누를 때 revealHint() 서버 액션으로 한 줄씩 받아 간다.
 *
 * ⚠️ 정답 문자열은 여기에도 두지 않는다. 정답은 서버 환경변수
 *    EVENT_CODENAME_ANSWER 에만 둔다.
 *
 * 화면에 몇 칸이 보이는지는 content.ts 의 HINT_MASKS 길이가 정한다.
 * 힌트를 늘리거나 줄이면 **두 배열의 길이를 같이** 맞춘다.
 */
export const HINTS = [
  "홍보물을 다시 봐라. 무지개색 순서를 따라가면 코드네임이 드러난다.",
  "그 조직은 요원에게 꽃의 이름을 코드네임으로 붙인다. 네가 찾는 건 꽃 이름이다.",
  "우리가 그를 부르는 호칭은 '요원 J'. 그 J가 이름의 첫 글자다.",
];
