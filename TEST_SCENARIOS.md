# 참가 신청 시스템 테스트 시나리오

비회원 구매 플로우(그룹 신청 + PII 암호화 + 참여내역 조회) 전체를 검증하기 위한 체크리스트. 로컬(`npm run dev`, `http://localhost:3000`)과 프로덕션(`https://wouldyouescape.com`) 양쪽에서 그대로 사용 가능. 홈 화면에서 "오후(비소개팅)"/"저녁(소개팅)" 두 회차 카드를 눌러 각 시나리오를 진행하면 된다.

**중요**: 아래 시나리오는 실제 신청 데이터를 만든다. 테스트가 끝나면 Supabase SQL Editor에서 만든 접수번호로 `delete from applications where confirmation_code in (...)`를 실행해 정리할 것 — 실제 8/22 행사 모집 현황에 테스트 데이터가 섞이면 안 된다.

---

## A. 신청 폼 — 기본 동작

### A1. 1인 신청 (골든패스)
1. 아무 회차 → "참가하기" → `/sessions/[id]/apply`로 이동. **로그인 화면으로 튕기지 않는지 확인.**
2. 인원 "1명" 유지, 대표 신청자 이름/전화번호/출생년도(1990~1999 중 선택) 입력, 닉네임은 비워둠.
3. 입금자명 입력, 약관 동의 체크.
4. "신청 제출" 클릭.
5. **기대 결과**: 접수번호(6자리 숫자) 발급, "참가확정" 배지, 참여자 명단에 본인 1명 표시, 무통장입금 안내(계좌 준비 중 문구), 하단에 "이 접수번호를 꼭 저장해두세요(클립보드에 복사됐어요)" 안내.
6. 브라우저 주소창 옆이나 아무 텍스트 입력창에 붙여넣기(Cmd+V) 해서 **실제로 클립보드에 접수번호가 복사됐는지 확인**.

### A2. 그룹 신청 (동행자 포함)
1. 다른 회차(또는 같은 회차, 아래 A5 자체중복만 아니면 무관)에서 인원수를 3~4명으로 변경.
2. 인원수를 바꿀 때마다 참여자 입력 카드가 정확히 그 수만큼 생기는지, 먼저 입력해둔 값이 유지되는지 확인(예: 4명 선택 후 입력하다가 3명으로 줄였다가 다시 4명으로 늘렸을 때 첫 3명 입력값이 남아있는지).
3. 각 참여자마다 서로 다른 이름/전화번호/출생년도, 일부만 닉네임 입력.
4. 제출 → **기대 결과**: 접수번호 발급, 참여자 명단에 전원(닉네임 있는 사람은 이름 옆에 괄호로 표시) + 대표 신청자만 "· 대표 신청자" 표기.

### A3. 정원 임박 상태 표시
1. 회차 상세 페이지(`/sessions/[id]`)에서 "조편성 정책" 영역에 현재 확정/대기 인원이 실제 신청 결과와 맞게 반영되는지 확인(A1, A2에서 신청한 인원수만큼 늘어나 있어야 함).

---

## B. 신청 폼 — 검증 / 에러 케이스

각 케이스마다 **제출 후에도 입력했던 값이 사라지지 않고 그대로 남아있는지**(이름/전화번호/출생년도/닉네임/입금자명/약관동의 체크박스 전부)를 반드시 같이 확인할 것 — 이게 이번에 고친 핵심 버그다.

### B1. 필수값 누락
- 대표 신청자 이름 또는 전화번호를 비운 채 제출 → "참여자 이름과 전화번호를 모두 입력해주세요." 에러, 폼 유지.
- 입금자명을 비운 채 제출 → "입금자명을 입력해주세요." 에러.
- 약관 동의 체크 안 하고 제출 → 브라우저 자체 필수값 경고(HTML `required`) 또는 서버 에러 확인.

### B2. 그룹 안 전화번호 자체 중복
1. 2명 이상 그룹에서 대표자와 동행자의 전화번호를 **똑같이** 입력 후 제출.
2. **기대 결과**: "그룹 안에서 전화번호가 중복돼요. 참여자별로 다른 전화번호를 입력해주세요." 에러 + 전화번호가 겹치는 **두 참여자 카드 모두** 빨간 테두리 + 카드 안에 "그룹 안의 다른 참여자와 전화번호가 중복돼요." 문구.
3. 겹치지 않는 번호로 하나만 고쳐서 재제출 → 정상 접수.

### B3. 동일 테마 재참여 차단
1. A1이나 A2에서 이미 신청에 쓴 전화번호 중 하나를 기억해둔다(예: 오후 회차/비소개팅에 쓴 번호).
2. **같은 테마의 다른 회차가 있다면 그 회차에서**, 없다면 같은 회차에서 그 전화번호로 새로 신청 시도.
3. **기대 결과**: "이미 같은 테마에 참여하신 분이 포함되어 있어요." 에러 + 해당 전화번호를 쓴 참여자 카드만 빨간 테두리 + "이미 같은 테마에 참여하신 신청자예요." 문구. 다른 참여자 카드는 하이라이트되지 않아야 함.
4. **다른 테마(오후 ↔ 저녁)** 회차에서는 같은 번호로 신청해도 **차단되지 않아야 함** — 정상 접수되는지 확인.

### B4. 닉네임 중복
1. 이미 쓰인 닉네임(A2에서 입력한 것)을 **같은 회차**에서 다시 사용 → "선택하신 닉네임 중 하나가 이미 사용 중이에요." 에러.
2. 같은 닉네임을 **다른 회차**에서 사용 → 정상 접수(회차 단위로만 유일하면 됨).
3. 닉네임을 아예 비워둔 여러 참여자를 한 그룹에 포함 → 정상 접수(빈 닉네임끼리는 중복 취급 안 함).

### B5. 출생년도 범위
- 신청 폼의 출생년도는 드롭다운이라 범위 밖의 값은 애초에 선택할 수 없음 — 정상 동작인지만 확인(소개팅은 드롭다운에 정확히 1990~2001만, 그룹/모임은 1987~2006만 있는지).

### B6. 정원 초과
1. 회차 하나가 정원(24명)에 가까울 때, 남은 자리보다 많은 인원으로 그룹 신청 시도(예: 남은 자리 3명인데 5명 그룹으로 신청).
2. **기대 결과**: "정원이 얼마 남지 않았어요. 인원을 줄이거나 다른 회차를 선택해주세요." 에러, 부분 확정 없이 전체 거부.
3. (참고) 정원까지 채워서 마감되는 것까지 재현하려면 실제로 다량의 테스트 신청이 필요해 이번 체크리스트에서는 필수는 아님 — 이전 세션에서 로직 자체는 검증됨.

### B7. 마감된 회차
- `sessions.status`가 `closed`인 회차가 있다면 상세 페이지에서 "모집이 마감되었습니다" 버튼 비활성 상태 확인, 신청 페이지 직접 접근 시에도 "이 회차는 모집이 마감되었습니다" 안내 확인.

---

## C. 참여내역 조회 (`/lookup`)

### C1. 정상 조회 — 대표 신청자 번호
1. A1 또는 A2에서 받은 접수번호 + 대표 신청자 전화번호로 조회.
2. **기대 결과**: 회차 정보, 상태(참가확정/대기) 배지, 입금 확인 배지, 접수번호, 입금자명, 참여자 명단(이름+닉네임, 대표 표시) 전부 정상 표시.

### C2. 정상 조회 — 동행자 번호
- A2 그룹 신청의 **동행자** 전화번호 + 같은 접수번호로 조회 → 대표자 번호가 아니어도 동일하게 조회되는지 확인(그룹 전체가 공유하는 정보이므로 됨).

### C3. 실패 케이스
- 맞는 전화번호 + 틀린 접수번호 → "일치하는 신청 내역을 찾을 수 없어요" (구체적으로 뭐가 틀렸는지 알려주지 않아야 함).
- 틀린 전화번호 + 맞는 접수번호 → 동일하게 실패 메시지만.
- 둘 다 틀림 → 동일.

---

## D. 보안 / 데이터 확인 (Supabase 대시보드)

### D1. 평문 노출 여부
1. Supabase → Table Editor → `application_attendees` 테이블 열람.
2. `name_enc`/`phone_enc` 컬럼이 사람이 읽을 수 없는 바이너리(암호문)로 보이는지 확인 — 이름/전화번호가 그대로 보이면 안 됨.
3. `applications` 테이블의 `depositor_name_enc`도 동일하게 확인.

### D2. 운영자용 복호화 뷰
1. SQL Editor에서 `select name, phone, nickname from admin_attendee_view order by created_at desc limit 5;` 실행.
2. 방금 테스트로 넣은 실제 이름/전화번호가 정상적으로 복호화되어 보이는지 확인(행사 운영 중 실제로 쓰게 될 조회 방법).

---

## E. 일반 UI

### E1. 헤더/네비게이션
- 어느 페이지에서든 헤더에 "로그인"/"회원가입"/"계정" 링크가 **없고**, "참여내역 조회" 링크만 있는지 확인.

### E2. 회차 상세 페이지
- 신청 자격 안내에 소개팅은 "1990~2001년생만 신청 가능", 그룹(모임)은 "20·30대 남녀만 신청 가능" 문구가 있는지("만 19세 이상" 문구가 아니라).
- "참가하기" 버튼이 로그인 여부와 무관하게 항상 `/sessions/[id]/apply`로 바로 연결되는지.

### E3. 홈 화면
- 오후/저녁 회차 카드와 실시간 모집 위젯(신청 N / 목표 20명 등)이 실제 신청 결과를 반영하는지.

---

## F. 프로덕션 전용 (`https://wouldyouescape.com`)

### F1. 접속/인증서
- `https://wouldyouescape.com` 및 `https://www.wouldyouescape.com` 접속 시 자물쇠 아이콘(유효한 SSL) 확인, `http://`로 접속해도 자동으로 `https://`로 리다이렉트되는지 확인(HSTS).

### F2. 핵심 플로우 1회 재현
- A1(1인 신청) + C1(조회)을 프로덕션에서 그대로 한 번 더 실행해 로컬과 동일하게 동작하는지 확인.

### F3. 정리
- 프로덕션에서 만든 테스트 신청도 잊지 말고 Supabase SQL Editor에서 삭제.

---

## 참고: 신청 시 화면에 나오는 에러 문구 모음 (정상 동작 확인용)

| 상황 | 문구 |
|---|---|
| 필수값 누락(이름/전화) | 참여자 이름과 전화번호를 모두 입력해주세요. |
| 입금자명 누락 | 입금자명을 입력해주세요. |
| 약관 미동의 | 약관에 동의해야 신청할 수 있습니다. |
| 출생년도 범위 밖(방어적, UI에선 발생 안 함) | 소개팅: 참여자 출생년도는 1990~2001년 범위만 가능합니다. / 그룹: 참여자 출생년도는 1987~2006년 범위만 가능합니다. |
| 그룹 내 전화번호 중복 | 그룹 안에서 전화번호가 중복돼요. 참여자별로 다른 전화번호를 입력해주세요. |
| 동일 테마 재참여 | 이미 같은 테마에 참여하신 분이 포함되어 있어요. |
| 닉네임 중복 | 선택하신 닉네임 중 하나가 이미 사용 중이에요. 다른 닉네임을 입력해주세요. |
| 정원 초과 | 정원이 얼마 남지 않았어요. 인원을 줄이거나 다른 회차를 선택해주세요. |
| 마감된 회차 | 이미 마감된 회차입니다. |
| 조회 실패(공통) | 일치하는 신청 내역을 찾을 수 없어요. 전화번호와 접수번호를 다시 확인해주세요. |
